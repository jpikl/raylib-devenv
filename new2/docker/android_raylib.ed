105,134c
when ODIN_ARCH == .i386 {
	foreign import lib "android/x86/libraylib.a"
} else when ODIN_ARCH == .amd64 {
	foreign import lib "android/x86_64/libraylib.a"
} else when ODIN_ARCH == .arm32 {
	foreign import lib "android/arm/libraylib.a"
} else when ODIN_ARCH == .arm64 {
	foreign import lib "android/arm64/libraylib.a"
.
w
